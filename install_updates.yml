---
- name: Windows Updates installieren
  hosts: windows
  gather_facts: false

  tasks:
    - name: Installiere alle verfügbaren Updates
      ansible.windows.win_updates:
        category_names: '*'
        reboot: true
      register: result

    - debug:
        var: result
